/* === app.js sederhana === */

console.log("✅ app.js loaded successfully");

// Tambahkan event sederhana untuk test JS aktif
document.addEventListener("DOMContentLoaded", () => {
    console.log("✅ DOM Loaded - Laravel app is running!");
});
